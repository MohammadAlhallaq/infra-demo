services:
  app:
    build: .
    container_name: laravel_app
    volumes:
      - .:/var/www

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf