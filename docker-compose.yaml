services:
  app:
    build: .
    image: infra-demo-app
    container_name: app
    volumes:
      - .:/var/www

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf